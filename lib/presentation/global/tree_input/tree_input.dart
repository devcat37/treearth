// Flutter imports:
import 'package:flutter/material.dart';

class TreeInput extends StatelessWidget {
  const TreeInput({Key? key}) : super(key: key);

  static const defaultHeight = 69.0;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
